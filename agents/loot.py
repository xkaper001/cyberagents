"""STUB for teammate. Copy exploitation.py's structure to fill this in."""
from agents.base import Agent


class LootAgent(Agent):
    name = "loot"
    description = "TODO (teammate): implement the loot phase."

    def run(self, target: str) -> None:
        print(f"[{self.name}] not implemented yet. Target was: {target}")
