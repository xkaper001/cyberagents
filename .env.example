# Default LLM credentials, loaded by secrets_embedded.py at runtime.
# Copy to `.env` and fill in. Ship a low-privilege, rotatable key ONLY.
LLM_BASE_URL=https://your-endpoint/openai/v1
LLM_API_KEY=your_key_here
LLM_MODEL=your_model
